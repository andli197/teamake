
;;; Code:

(defgroup cmake-project '()
  "CMake integration in Emacs using 'transient."
  :group 'tools)

(defgroup cmake-project-misc '()
  "Misc CMake project options."
  :group 'cmake-project)

(defgroup cmake-project-commands '()
  "Options for controlling the behavior of configurable commands."
  :group 'cmake-project)

(defgroup cmake-project-buffers '()
  "Options for cmake-project buffers."
  :group 'cmake-project)

(provide 'cmake-core)
;;; cmake-core.el ends here
